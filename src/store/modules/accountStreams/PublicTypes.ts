export enum ActionTypes {
  CREATE = "accountStreams/CREATE",
}

export enum GetterTypes {
  GET_ITEMS = "accountStreams/GET_ITEMS",
  GET_TOTAL = "accountStreams/GET_TOTAL",
  GET_PAGES = "accountStreams/GET_PAGES",
}
